# 学生管理系统 - 版本管理

## 当前版本

**v1.10.0**

---

## 版本历史

### v1.10.0 (2026-05-20)

**优化模块：**
- ✅ Prometheus监控指标优化 (`app.py`)
  - 为`flask_http_request_total`添加`status`标签，支持按状态码统计
  - 为`flask_http_request_duration_seconds`添加`status`标签
  - 新增系统状态指标：`app_up`、`total_users`、`total_students`
  - 指标每10秒自动更新，即使无请求也持续显示数据
  - 修复Grafana中"成功请求数(200)"显示N/A的问题

**优化模块：**
- ✅ 统一测试执行器增强 (`test_executor/test_runner.py`)
  - 端口检测从HTTP请求改为socket连接（更可靠、更快）
  - 修复进程清理逻辑，使用set避免重复PID处理
  - 添加PID有效性检查（必须为数字且大于0）
  - 优化命令执行方式，使用shell模式处理管道命令
  - 避免误杀PID为0的系统进程

---

### v1.9.2 (2026-05-18)
**新增模块：**
- ✅ Locust性能测试模块 (`performance_test_locust/`)
  - 纯Python性能测试方案
  - 支持Web UI模式和无UI模式
  - 与pytest生态集成更好
  - 支持CI/CD集成
  - 生成CSV格式测试报告

**优化模块：**
- ✅ 统一测试执行器增强 (`test_executor/`)
  - 集成Locust性能测试，自动检测Locust环境
  - 解析性能测试结果（请求数、失败数、响应时间、RPS）
  - 支持通过--stage performance单独运行性能测试

### v1.9.1 (2026-05-11)
**优化模块：**
- ✅ 统一测试执行器增强 (`test_executor/`)
  - 修复测试时间显示N/A问题（第一条用例开始时间，最后一条结束时间）
  - 邮件报告新增三个饼图：通过率、各阶段成功占比、失败统计
  - 修复自动化测试统计解析问题（live_output模式下stdout保存）
  - 邮件报告包含HTML正文（统计图表）和Markdown附件
  - 添加邮箱配置保护（email_config.py加入.gitignore）

### v1.9.0 (2026-05-11)
**优化模块：**
- ✅ 统一测试执行器优化 (`test_executor/`)
  - 添加实时输出，显示当前执行接口
  - 支持详细统计（成功/失败数量）
  - 优化自动化测试调用
  - 修复测试结果汇总显示
  - 自动启动/停止 Flask 应用
  - 测试完成后自动发送邮件报告（需配置 email_config.py）
  - 实现完整测试闭环：启动前检测端口占用，强制杀掉旧进程再启动

### v1.8.0 (2026-05-10)
**新增模块：**
- ✅ 安全测试模块 (`security_test/`)
  - SQL注入检测
  - XSS跨站脚本攻击检测
  - CSRF防护检测
  - 敏感信息泄露检测
  - 会话安全测试
  - 路径遍历攻击检测
  - 拒绝服务攻击测试
  - 密码安全测试

### v1.7.0 (2026-05-09)
**新增模块：**
- ✅ 集成测试模块 (`integration_test/`)
  - 用户注册登录完整流程测试
  - 学生管理生命周期测试
  - 会话持久化验证
  - 监控指标集成测试
  - 混沌测试集成测试

### v1.6.0 (2026-05-08)
**新增模块：**
- ✅ 功能测试用例文档 (`functional_test/`)
  - 57个详细功能测试用例
  - 覆盖8大功能模块
  - P0-P3优先级分级

### v1.5.0 (2026-05-07)
**新增模块：**
- ✅ API接口测试模块 (`api_test/`)
  - 20个API测试用例
  - 支持正常/异常场景
  - 自动生成中文测试报告
  - JSON和Markdown双格式输出

### v1.4.0 (2026-05-06)
**新增模块：**
- ✅ 混沌测试模块 (`chaos_test/`)
  - 延迟故障注入
  - 错误故障注入
  - 超时故障注入
  - 数据库故障注入

### v1.3.0 (2026-05-05)
**新增模块：**
- ✅ 监控模块 (`monitoring/`)
  - Prometheus指标配置
  - Grafana仪表盘
  - 请求指标采集
  - 耗时统计

### v1.2.0 (2026-05-04)
**新增模块：**
- ✅ 性能测试模块 (`performance_test_jmeter/`)
  - JMeter测试脚本
  - 数据清理脚本

### v1.1.0 (2026-05-03)
**新增模块：**
- ✅ 自动化测试模块 (`automation_test/`)
  - Playwright自动化测试
  - 浏览器自动化

### v1.0.0 (2026-05-02)
**初始版本：**
- ✅ 学生管理系统核心功能
- ✅ 用户认证（注册/登录/登出）
- ✅ 学生信息CRUD
- ✅ 单元测试模块

---

## 版本号规则

| 版本位 | 说明 | 示例 |
|--------|------|------|
| **主版本号** | 重大架构变更 | v**2**.0.0 |
| **次版本号** | 新增功能模块 | v1.**8**.0 |
| **修订号** | Bug修复和小改进 | v1.8.**1** |

---

## 模块清单

| 序号 | 模块名称 | 目录 | 状态 | 版本引入 |
|------|---------|------|------|---------|
| 1 | 核心应用 | `app.py` | ✅ | v1.0.0 |
| 2 | 单元测试 | `unit_test/` | ✅ | v1.0.0 |
| 3 | 自动化测试 | `automation_test/` | ✅ | v1.1.0 |
| 4 | JMeter性能测试 | `performance_test_jmeter/` | ✅ | v1.2.0 |
| 5 | 监控模块 | `monitoring/` | ✅ | v1.3.0 |
| 6 | 混沌测试 | `chaos_test/` | ✅ | v1.4.0 |
| 7 | API测试 | `api_test/` | ✅ | v1.5.0 |
| 8 | 功能测试 | `functional_test/` | ✅ | v1.6.0 |
| 9 | 集成测试 | `integration_test/` | ✅ | v1.7.0 |
| 10 | 安全测试 | `security_test/` | ✅ | v1.8.0 |
| 11 | 统一测试执行器 | `test_executor/` | ✅ | v1.9.0 |
| 12 | Locust性能测试 | `performance_test_locust/` | ✅ | v1.9.2 |
| 13 | 监控指标优化 | `app.py` | ✅ | v1.10.0 |