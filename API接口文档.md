# 学生管理系统 — API 接口文档

**基础地址**: `http://localhost:5000`  
**认证方式**: Session（Cookie 自动携带）  
**数据格式**: 表单提交（`application/x-www-form-urlencoded`）

---

## 目录

- [1. 用户认证](#1-用户认证)
  - [1.1 注册页面](#11-注册页面)
  - [1.2 提交注册](#12-提交注册)
  - [1.3 登录页面](#13-登录页面)
  - [1.4 提交登录](#14-提交登录)
  - [1.5 退出登录](#15-退出登录)
- [2. 学生管理](#2-学生管理)
  - [2.1 查看学生列表](#21-查看学生列表)
  - [2.2 添加学生](#22-添加学生)
  - [2.3 编辑学生页面](#23-编辑学生页面)
  - [2.4 提交编辑学生](#24-提交编辑学生)
  - [2.5 删除学生](#25-删除学生)
- [3. 监控接口](#3-监控接口)
  - [3.1 Prometheus 指标](#31-prometheus-指标)
- [4. 混沌测试接口](#4-混沌测试接口)
  - [4.1 获取混沌状态](#41-获取混沌状态)
  - [4.2 启动混沌测试](#42-启动混沌测试)
  - [4.3 停止混沌测试](#43-停止混沌测试)
  - [4.4 切换混沌状态](#44-切换混沌状态)
  - [4.5 更新混沌配置](#45-更新混沌配置)

---

## 1. 用户认证

### 1.1 注册页面

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/register` |
| 需要登录 | 否 |

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 返回注册页面 HTML |

---

### 1.2 提交注册

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/register` |
| Content-Type | `application/x-www-form-urlencoded` |
| 需要登录 | 否 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `username` | string | 是 | 用户名，不可为空 |
| `password` | string | 是 | 密码，不可为空 |

**请求示例**

```
POST /register HTTP/1.1
Content-Type: application/x-www-form-urlencoded

username=zhangsan&password=123456
```

**成功响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 注册成功，返回注册页面（含 Toast 提示"注册成功"） |

**失败响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 注册失败，返回注册页面（含错误提示） |

| 错误提示 | 触发条件 |
|----------|----------|
| `用户名和密码不能为空` | `username` 或 `password` 为空 |
| `用户名已存在` | `username` 已被注册 |

---

### 1.3 登录页面

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/login` |
| 需要登录 | 否（已登录则自动跳转首页） |

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `toast` | string | 否 | 页面顶部显示的提示信息 |

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 返回登录页面 HTML |
| `302` | 已登录时重定向到 `/` |

---

### 1.4 提交登录

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/login` |
| Content-Type | `application/x-www-form-urlencoded` |
| 需要登录 | 否 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `username` | string | 是 | 用户名 |
| `password` | string | 是 | 密码 |

**成功响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 登录成功，返回登录页面（含 Toast 提示"登录成功，正在跳转..."，页面自动跳转到首页） |

**失败响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 登录失败，返回登录页面（含错误提示） |

| 错误提示 | 触发条件 |
|----------|----------|
| `用户名或密码错误` | 用户名不存在或密码不匹配 |

---

### 1.5 退出登录

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/logout` |
| 需要登录 | 是 |

**响应**

| 状态码 | 说明 |
|--------|------|
| `302` | 清除会话，重定向到 `/login` |

---

## 2. 学生管理

> 以下接口均需要登录，未登录将重定向到 `/login`。

### 2.1 查看学生列表

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/` |
| 需要登录 | 是 |

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 返回学生列表页面 HTML，包含所有学生记录（按 ID 降序排列） |
| `302` | 未登录，重定向到 `/login` |

**页面数据**

模板渲染时传入：
- `students` — 学生列表，每项包含 `id`, `name`, `age`, `class_name`
- `user` — 当前登录用户信息，包含 `id`, `username`

---

### 2.2 添加学生

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/students` |
| Content-Type | `application/x-www-form-urlencoded` |
| 需要登录 | 是 |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `name` | string | 是 | 学生姓名，不可为空，最长 12 个字符 |
| `age` | int | 否 | 年龄，默认 `0` |
| `class_name` | string | 否 | 班级，默认为空 |

**请求示例**

```
POST /students HTTP/1.1
Content-Type: application/x-www-form-urlencoded

name=张三&age=18&class_name=高三(1)班
```

**成功响应**

| 状态码 | 说明 |
|--------|------|
| `302` | 添加成功，重定向到 `/` 显示更新后的列表 |

**失败响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 添加失败，返回首页（含错误提示） |

| 错误提示 | 触发条件 |
|----------|----------|
| `姓名不能为空` | `name` 为空 |
| `姓名长度不能超过12个字符` | `name` 超过 12 个字符 |

---

### 2.3 编辑学生页面

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/students/<student_id>/edit` |
| 需要登录 | 是 |

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `student_id` | int | 学生 ID |

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 返回编辑页面 HTML，表单预填该学生当前信息 |
| `302` | 未登录，重定向到 `/login` |

---

### 2.4 提交编辑学生

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/students/<student_id>` |
| Content-Type | `application/x-www-form-urlencoded` |
| 需要登录 | 是 |

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `student_id` | int | 学生 ID |

**请求参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `name` | string | 是 | 学生姓名 |
| `age` | int | 否 | 年龄 |
| `class_name` | string | 否 | 班级 |

**成功响应**

| 状态码 | 说明 |
|--------|------|
| `302` | 编辑成功，重定向到 `/` |

---

### 2.5 删除学生

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/students/<student_id>/delete` |
| 需要登录 | 是 |

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `student_id` | int | 学生 ID |

**响应**

| 状态码 | 说明 |
|--------|------|
| `302` | 删除成功，重定向到 `/` |

---

## 3. 监控接口

### 3.1 Prometheus 指标

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/metrics` |
| 需要登录 | 否 |

**响应**

| 状态码 | Content-Type | 说明 |
|--------|-------------|------|
| `200` | `text/plain; version=0.0.4; charset=utf-8` | Prometheus 格式指标文本 |

**返回指标列表**

| 指标名 | 类型 | 说明 |
|--------|------|------|
| `flask_http_request_total` | Counter | HTTP 请求总数（标签：`path`, `status`） |
| `flask_http_request_duration_seconds` | Histogram | HTTP 请求耗时（标签：`path`, `status`） |
| `register_requests_total` | Counter | 注册请求统计（标签：`status`: success/fail） |
| `login_requests_total` | Counter | 登录请求统计（标签：`status`: success/fail） |
| `add_student_requests_total` | Counter | 添加学生请求统计（标签：`status`: success/fail） |
| `in_flight_requests` | Gauge | 当前正在处理的请求数（标签：`path`） |
| `app_up` | Gauge | 应用运行状态（1=运行中） |
| `total_users` | Gauge | 注册用户总数 |
| `total_students` | Gauge | 学生总数 |

---

## 4. 混沌测试接口

> 混沌测试模块通过 Blueprint 注册，用于故障注入测试。

### 4.1 获取混沌状态

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `GET` |
| 路径 | `/chaos/status` |

**响应示例**

```json
{
  "enabled": false,
  "probability": 0.1,
  "fault_types": ["latency", "error"]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `enabled` | bool | 是否启用混沌测试 |
| `probability` | float | 故障注入概率（0.0~1.0） |
| `fault_types` | array | 支持的故障类型列表 |

---

### 4.2 启动混沌测试

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/chaos/start` |
| Content-Type | `application/json`（可选） |

**请求参数（可选）**

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `probability` | float | `0.1` | 故障注入概率 |

**请求示例**

```bash
# 默认 10% 故障概率
curl -X POST http://localhost:5000/chaos/start

# 指定 20% 故障概率
curl -X POST http://localhost:5000/chaos/start \
  -H "Content-Type: application/json" \
  -d '{"probability": 0.2}'
```

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 启动成功，返回当前状态 |

---

### 4.3 停止混沌测试

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/chaos/stop` |

**请求示例**

```bash
curl -X POST http://localhost:5000/chaos/stop
```

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 停止成功，返回当前状态 |

---

### 4.4 切换混沌状态

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `POST` |
| 路径 | `/chaos/toggle` |

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 切换成功，返回切换后的状态 |

---

### 4.5 更新混沌配置

**请求**

| 项目 | 说明 |
|------|------|
| 方法 | `PUT` |
| 路径 | `/chaos/config` |
| Content-Type | `application/json` |

**请求参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `probability` | float | 故障注入概率（0.0~1.0） |

**请求示例**

```bash
curl -X PUT http://localhost:5000/chaos/config \
  -H "Content-Type: application/json" \
  -d '{"probability": 0.3}'
```

**响应**

| 状态码 | 说明 |
|--------|------|
| `200` | 更新成功，返回新配置 |

---

## 附录

### 数据库表结构

**users 表**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | INTEGER | PRIMARY KEY, AUTOINCREMENT | 用户 ID |
| `username` | TEXT | NOT NULL, UNIQUE | 用户名 |
| `password` | TEXT | NOT NULL | 密码（SHA-256 哈希存储） |

**students 表**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | INTEGER | PRIMARY KEY, AUTOINCREMENT | 学生 ID |
| `name` | TEXT | NOT NULL | 学生姓名（最长 12 字符） |
| `age` | INTEGER | NOT NULL | 年龄 |
| `class_name` | TEXT | NOT NULL | 班级 |

### 认证机制

- 使用 Flask 内置 Session（Cookie-based）
- Session Cookie 名称：默认 `session`
- 密码存储：SHA-256 单向哈希，不存储明文
- 会话存储：服务端内存（Flask 默认）
- 未登录访问受保护接口时，返回 `302` 重定向到 `/login`

### 错误响应格式

本系统为服务端渲染应用，错误信息直接嵌入 HTML 页面返回。API 测试时可通过以下方式判断：

| 场景 | 判断方式 |
|------|----------|
| 注册/登录失败 | 响应体包含错误提示文本 |
| 未登录访问 | 响应状态码 `302`，`Location` 头指向 `/login` |
| 混沌测试注入 | 响应状态码 `500`/`503`/`408` |
